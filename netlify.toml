[build]
  base = "apps/web"
  command = "cd ../../ && npm run db:generate --workspace=@gastrosys/db && cd apps/web && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"
